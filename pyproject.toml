[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "model-converter"
version = "0.1.0"
description = "Safe parser-based PyTorch checkpoint converter to safetensors"
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3"]
module-name = "model_converter"
